name: endserver
description: eSys server.
version: 1.0.0
publish_to: none

environment:
  sdk: '^3.0.0'

dependencies:
  shelf: ^1.2.0
  shelf_router: ^1.0.0
  shelf_static: ^1.0.0
  common:
    path: ../common
  socket_io: ^1.0.1
  sqflite_common_ffi: ^2.3.0+2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
