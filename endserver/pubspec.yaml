name: endserver
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  shelf: ^1.2.0
  shelf_router: ^1.0.0
  shelf_static: ^1.0.0
  common:
    path: ../common
  socket_io: ^1.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
