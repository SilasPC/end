/// max time allowed in cooldown before vet gate
const COOL_TIME = Duration(minutes: 20);

/// max departure delay allowed
const MAX_DEPART_DELAY = Duration(minutes: 15);

/// TODO: not const
const REST_TIME = Duration(minutes: 40);

/// max heart rate allowed
const MAX_HEART_RATE = 60;
